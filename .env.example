# RefChecker Environment Configuration
# Copy this file to .env and fill in your values

# ============================================
# Server Configuration
# ============================================

# Port for the API proxy server (default: 5174)
PORT=5174

# GROBID service URL (default: http://localhost:8070)
# Start GROBID with: docker run -t --rm -p 8070:8070 lfoppiano/grobid:0.8.2
GROBID_URL=http://localhost:8070

# GROBID timeout in milliseconds (default: 120000 = 2 minutes)
GROBID_TIMEOUT_MS=120000

# ============================================
# API Keys (Server-Side - Secure)
# ============================================

# Perplexity API Key (for web search in agent-based validation)
# Get your key at: https://www.perplexity.ai/settings/api
PERPLEXITY_API_KEY=

# ============================================
# Notes on Client-Side API Keys
# ============================================
# The following API keys are configured in the UI Settings panel
# and stored in the user's browser localStorage:
#
# - OpenAI API Key (for LLM extraction)
# - Anthropic API Key (for LLM extraction)
# - Google Gemini API Key (for LLM extraction)
# - Semantic Scholar API Key (optional, for higher rate limits)
#
# These are NOT configured here because:
# 1. They are user-specific
# 2. They are sent directly to their respective APIs
# 3. They should be managed by each user
